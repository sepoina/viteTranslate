<!-- vitetranslate:generated 2026-09-18 · 8 keys · deepseek-flash -->
## Domain
A translation management tool for a Vite-based project, driven by a CLI called `vitetranslate`. It syncs translation tables, fills empty entries via an LLM translation run, and validates placeholders and tags against the source.

## Register
Informal and direct: second-person singular address ("ti mostra", "ti chiede"), as if explaining to a developer. Technical but conversational, with no formal register.

## Glossary
vitetranslate
`--translate`
vite.config.js
doc/llm.md
CLI
API
null
%s
<b>
&nbsp;

## Ambiguities
"mette le stringhe nuove a null" — whether the literal word `null` is written into the table or the cell is left empty.
"le caselle vuote" / "caselle" — table cells versus form fields or checkboxes.
"il modello" — the LLM model, not a template or data model; unclear from the string alone.
"Il testo si scrive dove serve, fra due marcatori" — which markers are meant is not stated.
"%s lingue · versione&nbsp;<b>%s</b>" — two `%s` placeholders with different meanings (count, version); order and count must be preserved.
"seg naposto e tag" — `tag` could mean markup tags or validation labels.
<!-- /vitetranslate:generated -->

## Notes from the team
<!-- Everything outside the generated block is yours and is never overwritten. -->
