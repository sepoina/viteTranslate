# Piano di implementazione 4.1.1

> [!NOTE]
> **Per il revisore umano**
> - **Il problema**: cinque costanti di contratto sono scritte in due o tre punti diversi —
>   i delimitatori del marcatore, `__untranslated__`, `%s`, la regex della chiave di tabella,
>   il nome del comando. Tre attraversano il confine build/runtime, dove una divergenza non
>   dà nessun errore: dà testo sbagliato.
> - **Il secondo problema**: `updateLanguage.js` e `updateAllSubLanguages.js` contengono due
>   copie della stessa sequenza (leggi, classifica il guasto, fai il backup / serializza,
>   confronta i byte, scrivi se cambiato). Le due copie sono già leggermente divergenti.
> - **Il terzo**: `vitetranslate.js` (786 righe) e `cli.js` (623) tengono insieme cose
>   separabili. Si spacchettano senza cambiare una riga di comportamento.
> - **La mossa**: un modulo condiviso `lib/markerSyntax.js` (stesso ruolo di `htmlDialect.js`),
>   due helper di sync, sei moduli estratti dai due file lunghi.
> - **Costo**: nessuna opzione nuova, nessuna firma pubblica cambiata, nessun file di lingua
>   che cambia forma. Il `dist` deve restare della stessa dimensione.
> - **L'unica cosa che chiede attenzione in review**: Punto 6c, l'`handler` del transform dei
>   file di lingua usa `this` di Rollup e non può diventare una arrow function.

> [!IMPORTANT]
> **Log delle sette fasi**
> - Implementazione: le sette fasi seguite in ordine, tutte e sette i punti applicati senza
>   scostamenti dal testo del piano.
> - `markerSyntax.js` è ora il terzo file condiviso build/runtime, accanto a `htmlDialect.js`
>   ed `errorSolve.js`. Le sue costanti vengono inlineate come letterali dal bundler (verificato
>   sul bundle): nessun modulo in più a runtime.
> - `vitetranslate.js` 786→339 righe, `cli.js` 623→312, spacchettati in 10 nuovi file
>   (`markerSyntax.js`, `buildManifest.js`, `compileLocale.js`, `uty/{cliName,posix,
>   readLanguageForSync,writeLanguageFile,setupFailure,loadConfig,scanSource,syncReport}.js`).
> - Punto 6c: l'`handler` di `compileLocale.js` è rimasto un metodo `function` (non arrow),
>   come richiesto — usa `this.addWatchFile` di Rollup.
> - Punto 5.2: `markerCore.relPathOf` lasciato fuori dalla deduplica di `toPosix` di proposito
>   (entra nel checksum degli id dei marcatori).
> - **Build**: `react.es.js`/`.cjs` invariati o leggermente calati (-50 B ciascuno, entro
>   tolleranza). `vitetranslate.es.js`/`.cjs` cresciuti di ~7,8 KB (85,3→93,1 KB; 86,8→94,6 KB),
>   ben oltre la tolleranza dichiarata di ±200 B: interamente nuova documentazione nei moduli
>   estratti, nessuna duplicazione di logica (verificato confrontando i due bundle via `git
>   stash`), dump del playground byte-identico. **Sottoposto in ask, l'utente ha accettato la
>   crescita**: nessuna riscrittura di codice.
> - Un solo test preesistente toccato (`babelTranslateReference.mjs`, previsto dal Punto 1.11
>   del piano stesso). Suite finale: 29/29 test, 1174 asserzioni (+60 sulla baseline di 1114).

---

## Procedura di lavoro

Vale `AGENTS.md` § REGOLE DI IMPLEMENTAZIONE DEL PLAN. Le sette fasi sono **implementazione,
test, build, revisione, documentazione, pulizia, logDiary**, in quest'ordine, e non si salta
avanti.

- I test che si rendono necessari strada facendo si **appuntano** in `4_1_1.necessarytest.md`
  e si scrivono tutti insieme in fase 2, non mentre si implementa.
- Le modifiche alla documentazione si **appuntano** in `4_1_1.necessarydoc.md` e si eseguono
  tutte insieme in fase 5.
- Ogni contraddizione fra questo piano e i sorgenti è un **ask all'utente**, non una decisione
  da prendere sul posto.
- Le build si evitano finché possibile: in fase 1 e 2 si verifica con `node test/run.mjs`, che
  gira sui sorgenti e non sul bundle.

La nota `> [!TIP]` con il log delle sette fasi si scrive **alla fine**, in fase 7, subito sotto
la nota per il revisore umano. Non scriverla adesso.

**Baseline da cui si parte** (misurata prima di scrivere questo piano — serve in fase 3 per
sapere se qualcosa è cambiato):

| Cosa | Valore |
|---|---|
| `node test/run.mjs` | 25/25 test, 1114 asserzioni |
| `lib/dist/react.es.js` | 42.507 byte |
| `lib/dist/react.cjs` | 44.516 byte |
| `lib/dist/vitetranslate.es.js` | 85.319 byte |
| `lib/dist/vitetranslate.cjs` | 86.761 byte |

---

## Il problema, in concreto

Le costanti scritte due volte. La colonna a destra è cosa succede se le due copie divergono —
e in nessuno dei cinque casi succede un errore: succede un risultato sbagliato in silenzio.

| Costante | Dove sta oggi | Se divergono |
|---|---|---|
| `_%_` (apre/chiude il marcatore sorgente) | `dev/babel/markerCore.js` (`OPEN`/`CLOSE`) e `react/parseCompiledMarker.js` (`SOURCE_OPEN`/`SOURCE_CLOSE`) | il runtime non toglie più i delimitatori: l'utente finale legge `_%_Benvenuto_%_` |
| `_<_` `_/_` `_>_` (marcatore compilato) | scritti da `markerCore.compiledMarker()`, riletti da `parseCompiledMarker.js` con il numero magico `3`, e ricontrollati a mano in `react/Translate.js:31` e `react/useTranslateToString.js:106` | la chiave estratta è sbagliata: ogni stringa cade nel fallback |
| `__untranslated__` | `dev/compile/compileTable.js:102` e `react/resolveEntry.js:24` | il prefisso 🔸 non compare più, e nessuno se ne accorge |
| `%s` | `dev/compile/compileTable.js:49` e `react/interpolate.js:53,58` | segnaposto interpolati da una parte e mostrati grezzi dall'altra |
| `/^[A-Za-z_][A-Za-z0-9_.-]*$/` | `uty/parseLanguageFile.js:57,85` (lettore) e `uty/serializeLanguageFile.js:13` (scrittore) | si scrive un file di lingua che alla sync successiva non si rilegge |
| `"vtranslate-cli"` | `dev/vite/cli.js:351`, `dev/vite/vitetranslate.js:26`, `uty/languageStatus.js:15` | si suggerisce all'utente un comando che non esiste |

I blocchi ripetuti:

- **lettura con quattro guasti**: `updateLanguage.js:59-106` e `updateAllSubLanguages.js:83-119`
  fanno la stessa cosa (leggi il testo, prova il parse, distingui illeggibile / corrotto /
  vuoto / a posto, azzera `oldText` a ogni guasto perché forzi la riscrittura).
- **scrittura se cambiata**: `updateLanguage.js:129-152` e `updateAllSubLanguages.js:147-156`
  fanno la stessa cosa (`splitAndSortEntries` → `serializeLanguageFile` →
  `sameIgnoringProcessed` → `writeFileSync`). La stessa terna compare anche in
  `vitetranslate.js:296-300` e `uty/migrateLegacyLanguages.js:78-83`.
- **normalizzazione a `/`**: `.replace(/\\/g, "/")` in nove punti.
- **riga `$ comando`**: `cli.js:447`, `vitetranslate.js:695`, `uty/languageStatus.js:181`,
  ognuna preceduta dalle stesse sette righe di commento che ne spiegano la convenzione.

---

## Invarianti da NON rompere in questo piano

Sono tutte verificabili. Se una di queste cade, ci si ferma e si passa alla fase 4 (revisione).

1. **Nessuna firma pubblica cambia.** `lib/index.js` e `lib/react/index.js` esportano
   esattamente gli stessi nomi di adesso. `lib/index.d.ts`, `lib/react.d.ts` e
   `lib/virtual.d.ts` non si toccano affatto.
2. **Nessun output a schermo cambia**, byte per byte. `test/list/logFormat.test.mjs`,
   `fastVerifyCli.test.mjs` e `syncPipeline.test.mjs` confrontano l'output del comando: se
   cambia, non è un test da aggiornare, è una regressione.
3. **Nessun file di lingua cambia forma.** `test/list/headerContract.test.mjs` e
   `languageFileIO.test.mjs` sono la prova.
4. **Gli id dei marcatori non cambiano.** Sono `hash(inner, hash(relPath))`: per questo
   `markerCore.relPathOf` **non si tocca** in questo piano (vedi Punto 5b).
5. **`lib/markerSyntax.js` non importa niente**, esattamente come `htmlDialect.js` e
   `errorSolve.js`: lo leggono sia il lato Node sia il lato browser.
6. **Niente `Object.freeze` in `markerSyntax.js`.** Vedi il commento in testa a
   `errorSolve.js`: una `Object.freeze` non annotata è una chiamata con effetti, il bundler la
   tiene, e le costanti finiscono nel bundle di produzione anche dove nessuno le legge.
7. **Le fasi si rispettano.** Niente build in fase 1, niente documentazione in fase 2.

---

# FASE 1 — Implementazione

Sette punti, in quest'ordine. L'ordine conta: il Punto 6 e il Punto 7 spostano codice che i
punti precedenti hanno già modificato, e farli prima vorrebbe dire farli due volte.

Dopo **ogni** punto: `node test/run.mjs`. Deve restare 25/25. Se un test rompe, si sistema
prima di passare al punto successivo.

---

## Punto 1 — `lib/markerSyntax.js`: la sintassi del marcatore in un posto solo

### 1.1 Creare il file

Nuovo file `lib/markerSyntax.js`. Sta nella **radice** di `lib/`, accanto a `htmlDialect.js` e
`errorSolve.js`, e per la stessa ragione: lo leggono entrambi i lati.

Contenuto (i commenti vanno scritti, sono la parte che spiega perché il file esiste):

```js
// Architettura d'insieme: doc/structure.md § "Fase 0 — Autoring: il marcatore" e
// § "Fase 4 — Runtime: la catena di risoluzione".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

// La sintassi del marcatore in un posto solo, come per il dialetto HTML (lib/htmlDialect.js)
// e per errorSolve (lib/errorSolve.js).
//
// Chi la scrive e chi la legge sono due programmi diversi: il marcatore sorgente lo scrive
// l'utente e lo legge l'estrazione (lato Node), il marcatore compilato lo scrive l'estrazione
// e lo legge il runtime (lato browser). Finché i delimitatori erano scritti a mano da
// entrambe le parti, la prima divergenza non avrebbe prodotto nessun errore: avrebbe prodotto
// testo sbagliato a schermo — i delimitatori interni mostrati all'utente finale, o ogni
// stringa che cade nel fallback.
//
// Il file non importa nulla e non dipende né da React né da Node: entrambi i lati lo prendono
// così com'è. Niente Object.freeze: vedi la nota in errorSolve.js sul perché una freeze non
// annotata trascina le costanti nel bundle di produzione.

// --- Marcatore SORGENTE: quello che si scrive nel codice, `_%_ciao_%_` ---

export const SOURCE_OPEN = "_%_";
export const SOURCE_CLOSE = "_%_";

/** "_%__%_": marcatore vuoto, la stringa marcata più corta che possa esistere. Sotto questa
 *  soglia un "_%_" isolato aprirebbe e chiuderebbe se stesso. */
export const MIN_SOURCE_MARKED = SOURCE_OPEN.length + SOURCE_CLOSE.length;

// --- Marcatore COMPILATO: quello che l'estrazione mette al suo posto ---
//
// `_<_id_/_fallback_>_` in sviluppo (il testo sorgente resta a portata di mano prima che una
// sync abbia popolato i file di lingua), `_<_id_>_` in build (il comando vtranslate-cli gira
// prima, quindi il fallback in bundle sarebbe ridondante).

export const COMPILED_OPEN = "_<_";
export const COMPILED_SEP = "_/_";
export const COMPILED_CLOSE = "_>_";

/** Costruisce il marcatore compilato. Lo legge `parseCompiledMarker.js`. */
export function compiledMarker(id, inner, includeFallback) {
  return includeFallback
    ? `${COMPILED_OPEN}${id}${COMPILED_SEP}${inner}${COMPILED_CLOSE}`
    : `${COMPILED_OPEN}${id}${COMPILED_CLOSE}`;
}

/** La stringa è un marcatore compilato? Il controllo che due emitter facevano a mano. */
export const isCompiledMarker = (text) =>
  text.startsWith(COMPILED_OPEN) && text.endsWith(COMPILED_CLOSE);

// --- Segnaposto degli argomenti ---
//
// Due lettori: il compilatore delle tabelle, che lo spezza (`split`), e l'interpolazione a
// runtime, che lo sostituisce (`replace`). Le due forme stanno su righe adiacenti proprio
// perché descrivono lo stesso token e devono cambiare insieme.

export const PLACEHOLDER = "%s";
export const PLACEHOLDER_RE = /%s/g;

// --- Chiave riservata delle tabelle compilate ---
//
// L'elenco delle voci che in questa lingua una traduzione non ce l'hanno. La scrive
// compileTable.js, la legge resolveEntry.js per decidere il prefisso 🔸. Le chiavi vere sono
// `Basename_hash` e non possono collidere con questa.

export const UNTRANSLATED_KEY = "__untranslated__";
```

> **Nota, così non ci si spaventa**: questo file contiene la stringa letterale `"_%_"`, quindi
> il transform del plugin lo pre-seleziona (`filter: { code: "_%_" }`). Non è un problema: la
> guardia `OWN_LIB_DIR` in `vitetranslate.js` scarta tutto ciò che sta dentro `lib/` del
> pacchetto stesso, ed è già così per `errorSolve.js` e `interpolate.js`. Non c'è niente da
> aggiungere.

### 1.2 `lib/dev/babel/markerCore.js`

- **Togliere** le definizioni di `OPEN`, `CLOSE`, `MIN_MARKED_LENGTH` e la funzione
  `compiledMarker` (righe 31-34 e 71-79).
- **Aggiungere** in cima: `import { SOURCE_OPEN, SOURCE_CLOSE, MIN_SOURCE_MARKED } from "../../markerSyntax.js";`
- In `markedTextOf`, sostituire `MIN_MARKED_LENGTH` → `MIN_SOURCE_MARKED`, `OPEN` → `SOURCE_OPEN`,
  `CLOSE` → `SOURCE_CLOSE`.
- In `innerTextOf`, sostituire `OPEN.length` → `SOURCE_OPEN.length` e `-CLOSE.length` →
  `-SOURCE_CLOSE.length`.
- In `registerMarker`, sostituire `inner.includes(OPEN)` → `inner.includes(SOURCE_OPEN)`.
- **Non aggiungere re-export di comodo.** I due importatori si aggiornano (1.3 e Fase 2).

### 1.3 `lib/dev/babel/extractMarkers.js`

- Nell'import da `./markerCore.js`, togliere `compiledMarker` e `OPEN`.
- Aggiungere: `import { compiledMarker, SOURCE_OPEN } from "../../markerSyntax.js";`
- Sostituire le due occorrenze di `OPEN` (riga ~139 `raw.includes(OPEN)` e riga ~144, dentro il
  messaggio `contains "${OPEN}"`) con `SOURCE_OPEN`. **Il testo del messaggio deve restare
  identico**: `SOURCE_OPEN` vale `"_%_"`, quindi lo è.

### 1.4 `lib/react/parseCompiledMarker.js`

- Aggiungere: `import { COMPILED_OPEN, COMPILED_SEP, COMPILED_CLOSE, SOURCE_OPEN, SOURCE_CLOSE, MIN_SOURCE_MARKED } from "../markerSyntax.js";`
- **Togliere** le costanti locali `SOURCE_OPEN`, `SOURCE_CLOSE`, `MIN_SOURCE_MARKED` (righe 61-64).
- In `markerKey`, i due numeri magici sono la lunghezza dei delimitatori:
  ```js
  const sep = text.indexOf(COMPILED_SEP, COMPILED_OPEN.length);
  const key = sep === -1
    ? text.slice(COMPILED_OPEN.length, -COMPILED_CLOSE.length)
    : text.slice(COMPILED_OPEN.length, sep);
  ```
- In `markerFallback`, stessa cosa:
  ```js
  const sep = text.indexOf(COMPILED_SEP, COMPILED_OPEN.length);
  return sep === -1 ? undefined : text.slice(sep + COMPILED_SEP.length, -COMPILED_CLOSE.length);
  ```
  **Attenzione**: `sep + 3` diventa `sep + COMPILED_SEP.length`, non `sep + COMPILED_OPEN.length`.
  Sono entrambi 3, ma il 3 che conta lì è la lunghezza del **separatore**.
- `stripSourceMarker` resta com'è: usa già le costanti, ora importate.
- `test/list/markerCache.test.mjs` copre tutti questi casi. Se passa, la sostituzione è giusta.

### 1.5 `lib/react/Translate.js`

- **Togliere** la riga 31 (`const isCompiledMarker = ...`) e il commento sopra.
- Aggiungere `import { isCompiledMarker } from "../markerSyntax.js";`
- Le due chiamate a `isCompiledMarker(text)` restano identiche.

### 1.6 `lib/react/useTranslateToString.js`

- Aggiungere `import { isCompiledMarker } from '../markerSyntax.js';`
- Sostituire `if (text.startsWith('_<_') && text.endsWith('_>_')) {` con
  `if (isCompiledMarker(text)) {`.

### 1.7 `lib/dev/compile/compileTable.js`

- **Togliere** `const PLACEHOLDER = "%s";` (riga 49) e `const UNTRANSLATED_KEY = "__untranslated__";`
  (riga 102, con il suo commento — il commento si sposta in `markerSyntax.js`, dove è già
  scritto al Punto 1.1).
- Aggiungere `import { PLACEHOLDER, UNTRANSLATED_KEY } from "../../markerSyntax.js";`

### 1.8 `lib/react/resolveEntry.js`

- **Togliere** `const UNTRANSLATED_KEY = "__untranslated__";` (riga 24, col commento).
- Aggiungere `import { UNTRANSLATED_KEY } from "../markerSyntax.js";`

### 1.9 `lib/react/interpolate.js`

- Aggiungere `import { PLACEHOLDER, PLACEHOLDER_RE } from "../markerSyntax.js";`
- `if (!text.includes("%s")) return text;` → `if (!text.includes(PLACEHOLDER)) return text;`
- `return text.replace(/%s/g, () => {` → `return text.replace(PLACEHOLDER_RE, () => {`
- **Perché è sicuro condividere una regex `/g` fra chiamate**: `String.prototype.replace` con
  una regex globale azzera `lastIndex` prima di cominciare e non lascia stato dietro di sé.
  Vale solo per `replace`: se un giorno qualcuno usasse `PLACEHOLDER_RE.test()` o `.exec()`,
  quello sì che porterebbe stato. Scrivilo come commento accanto all'uso.

### 1.10 I tre `code.includes("_%_")` sparsi

Sostituire con `SOURCE_OPEN`, importandolo da `markerSyntax.js`:

- `lib/dev/vite/vitetranslate.js:588` → `if (!code.includes(SOURCE_OPEN)) return null;`
- `lib/dev/vite/vitetranslate.js:573` → `filter: { code: SOURCE_OPEN },`
  (è una stringa passata al filtro di Rolldown: una costante importata va benissimo)
- `lib/dev/vite/cli.js:177` → `if (!code.includes(SOURCE_OPEN)) continue;`
- `lib/dev/vite/uty/fastVerify.js:156` → `const hasMarker = code.includes(SOURCE_OPEN);`

### 1.11 Verifica del punto

`node test/run.mjs` → 25/25. Uno dei test (`extractMarkers`) fallirà se
`test/list/babelTranslateReference.mjs` non è stato aggiornato: quel file importa
`compiledMarker` da `markerCore.js`, che ora non lo esporta più. **È l'unica modifica a un
file di test prevista in fase 1**; vale la pena farla subito invece di aspettare la fase 2:

```js
// test/list/babelTranslateReference.mjs, righe 1-3
import {
  markedTextOf, innerTextOf, escapeTemplateRaw, registerMarker,
} from "../../lib/dev/babel/markerCore.js";
import { compiledMarker } from "../../lib/markerSyntax.js";
```

---

## Punto 2 — la chiave del file di lingua, scritta una volta

Oggi il lettore e lo scrittore descrivono la stessa forma di chiave con due regex battute a
mano. Vanno a una sola.

### 2.1 `lib/dev/vite/uty/languageFileFormat.js`

Aggiungere in fondo al file (che continua a **non importare niente**):

```js
/**
 * La forma di una chiave in un file di lingua, come pattern e come regex ancorata.
 *
 * Il pattern grezzo serve perché parseLanguageFile deve incastonarlo dentro la regex di una
 * riga intera (ENTRY_RE), e la regex ancorata perché il lettore e lo scrittore la applicano
 * da soli sulla sola chiave. Erano scritte a mano in due file — il lettore e lo scrittore del
 * formato — e due copie divergenti vogliono dire scrivere un file che alla sync successiva
 * non si rilegge.
 *
 * Le chiavi generate (`Basename_checksum`, vedi markerCore.sanitizeName) rientrano sempre
 * qui. Trattino e punto restano ammessi in lettura per i file scritti da versioni precedenti.
 * I due punti non possono farne parte: è questo che rende sicuro tagliare la riga al primo ":".
 */
export const LANGUAGE_KEY_PATTERN = "[A-Za-z_][A-Za-z0-9_.-]*";
export const LANGUAGE_KEY_RE = new RegExp(`^${LANGUAGE_KEY_PATTERN}$`);
```

### 2.2 `lib/dev/vite/uty/parseLanguageFile.js`

- Aggiungere `import { LANGUAGE_KEY_PATTERN, LANGUAGE_KEY_RE } from "./languageFileFormat.js";`
- Riga 57, `ENTRY_RE` diventa:
  ```js
  export const ENTRY_RE = new RegExp(`^(${LANGUAGE_KEY_PATTERN}):(?:[ \\t]+(.*))?$`);
  ```
  **Attenzione al doppio backslash.** Dentro un template literal, `\t` diventerebbe un TAB vero
  e la regex riceverebbe un carattere di tabulazione al posto della sequenza `\t`. Nella
  classe `[ \t]` un TAB vero funzionerebbe comunque, ma non è quello che c'era scritto: si usa
  `\\t`, che consegna alla regex esattamente `\t`. Se sbagli qui,
  `test/list/languageFileIO.test.mjs` (che confronta riga per riga cosa accettano `ENTRY_RE` e
  il ciclo del parser) te lo dice.
- Riga 85, **togliere** `const KEY_RE = /^[A-Za-z_][A-Za-z0-9_.-]*$/;` e sostituire i suoi usi
  (`KEY_RE.test(key)`) con `LANGUAGE_KEY_RE.test(key)`.

### 2.3 `lib/dev/vite/uty/serializeLanguageFile.js`

- Aggiungere `import { LANGUAGE_KEY_RE } from "./languageFileFormat.js";`
- **Togliere** `const KEY_RE = ...` (riga 13) e il commento sopra, che ora è scritto in
  `languageFileFormat.js`.
- Sostituire `KEY_RE.test(key)` → `LANGUAGE_KEY_RE.test(key)` e, nel messaggio d'errore,
  `${KEY_RE}` → `${LANGUAGE_KEY_RE}`. La stringificazione di una regex costruita con
  `new RegExp` è identica a quella di un letterale: `/^[A-Za-z_][A-Za-z0-9_.-]*$/`. Nessun test
  asserisce su quel messaggio, ma il testo non deve cambiare comunque.

### 2.4 Verifica del punto

`node test/run.mjs` → 25/25, con particolare attenzione a `languageFileIO` (115 asserzioni).

---

## Punto 3 — `uty/cliName.js`: il nome del comando

### 3.1 Creare il file

Nuovo file `lib/dev/vite/uty/cliName.js`. Il commento è quello che oggi sta in `cli.js:343-350`,
spostato qui:

```js
// Architettura d'insieme: doc/structure.md § "Fase 1 — Precompilazione: il comando di sync".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

/**
 * Il nome con cui il comando si presenta. Dalla 4.1 il bin è `vtranslate-cli`: si scrive dopo
 * un `npx` decine di volte al giorno, e il nome vecchio —
 * `vitetranslate-prepare-translation-table` — descriveva bene quello che fa e malissimo quanto
 * costa digitarlo. Resta registrato come alias in package.json, così un `prebuild` già scritto
 * non si rompe; nei messaggi però ne compare uno solo, perché un comando che si autonomina in
 * due modi diversi è peggio di uno che ne sceglie uno.
 *
 * File suo, invece di stare dentro cli.js, per la stessa ragione di builderVersion.js e
 * configFiles.js: lo devono leggere anche languageStatus.js e vitetranslate.js, e importarlo
 * da cli.js — che a sua volta importa languageStatus.js — chiuderebbe un ciclo.
 */
export const CLI_NAME = "vtranslate-cli";
```

### 3.2 I tre consumatori

- `lib/dev/vite/cli.js`: togliere `const CLI_NAME = ...` (riga 351) e il commento sopra;
  aggiungere `import { CLI_NAME } from "./uty/cliName.js";`
- `lib/dev/vite/vitetranslate.js`: togliere `const CLI_NAME = ...` (riga 26) e il commento
  sopra; aggiungere `import { CLI_NAME } from "./uty/cliName.js";`
- `lib/dev/vite/uty/languageStatus.js`: togliere `const CLI_NAME = ...` (riga 15) e il commento
  sopra; aggiungere `import { CLI_NAME } from "./cliName.js";`

### 3.3 Cosa NON toccare

`uty/devReporter.js` continua a ricevere `cliName` come parametro. **Non** sostituirlo con un
import: i test lo passano esplicitamente (`test/list/devReporter.test.mjs`, dieci chiamate), e
un parametro esplicito è anche più onesto per una funzione che quel nome lo stampa e basta. In
`vitetranslate.js` la chiamata `creaReporter({ baseDir, cliName: CLI_NAME })` ora passa la
costante importata invece del letterale locale: è l'unico cambiamento.

---

## Punto 4 — lettura e scrittura condivise nella sync

Il pezzo più delicato del piano. Si estraggono due funzioni; **le parole dei messaggi restano
dove sono**, perché è lì che i due chiamanti divergono per davvero — stesso principio di
`checkSetup` (che restituisce `{ ok, reason }` e lascia a `setupErrorText` le frasi) e di
`readSource.js` (che restituisce un verdetto e lascia a ogni emitter cosa farne).

### 4.1 Nuovo `lib/dev/vite/uty/readLanguageForSync.js`

```js
// Architettura d'insieme: doc/structure.md § "Fase 1 — Precompilazione: il comando di sync",
// "Empty, emptied, unreadable".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

import fs from "fs";
import readLanguageFile, { readLanguageText } from "./readLanguageFile.js";

/**
 * Legge un file di lingua per la sincronizzazione e ne CLASSIFICA lo stato, senza decidere
 * cosa farne. Le parole — "was corrupted, rebuilt", "cannot be read, left untouched" — restano
 * a chi chiama: la lingua sorgente e una sub-lingua dicono cose diverse degli stessi guasti, e
 * su un file illeggibile prendono decisioni opposte (la prima prosegue con la scansione del
 * codice, la seconda salta il file).
 *
 * Era scritta due volte, in updateLanguage.js e updateAllSubLanguages.js, e le due copie
 * erano già leggermente divergenti.
 *
 * **L'invariante che questa funzione esiste per garantire**: `oldText` è il testo su disco
 * SOLO quando `status === "ok"`. In ogni altro caso è `null`, e quel `null` è ciò che più a
 * valle forza la riscrittura del file (vedi writeLanguageFileIfChanged). Tenerlo a mano in due
 * posti era il modo di dimenticarselo in uno dei due.
 *
 * @param {string} filePath
 * @returns {{ status: "missing"|"unreadable"|"corrupted"|"empty"|"ok",
 *   table?: object, meta?: { tableVersion: number|null }, oldText: string|null, error?: Error }}
 *   `error` c'è solo su "unreadable" e "corrupted"; su "corrupted" porta con sé
 *   `error.sourceText`, il testo da cui l'errore è nato (serve al backup — vedi
 *   readLanguageFile.js sul perché non si rilegge il disco una seconda volta).
 */
export default function readLanguageForSync(filePath) {
  if (!fs.existsSync(filePath)) return { status: "missing", oldText: null };

  let oldText;
  try {
    oldText = readLanguageText(filePath);
  } catch (error) {
    // Non sappiamo cosa contenga (una cartella con quel nome, i permessi, un link rotto).
    return { status: "unreadable", oldText: null, error };
  }

  let table, meta;
  try {
    ({ table, meta } = readLanguageFile(filePath, oldText));
  } catch (error) {
    return { status: "corrupted", oldText: null, error };
  }

  // File vuoto: è il modo documentato per aggiungere una lingua, non un guasto.
  if (table === undefined) return { status: "empty", oldText: null };

  return { status: "ok", table, meta, oldText };
}
```

### 4.2 Nuovo `lib/dev/vite/uty/writeLanguageFile.js`

```js
// Architettura d'insieme: doc/structure.md § "Fase 1 — Precompilazione", "Il file di lingua prodotto".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

import fs from "fs";
import splitAndSortEntries from "./splitAndSortEntries.js";
import serializeLanguageFile from "./serializeLanguageFile.js";
import { sameIgnoringProcessed } from "./buildLanguageHeader.js";

/**
 * Ordina, serializza e scrive un file di lingua — ma solo se i byte sono davvero cambiati.
 *
 * La terna ordina/serializza/confronta compariva in quattro punti (le due sync, il bootstrap
 * del plugin, la migrazione), e il confronto è la parte che è facile scrivere quasi giusta: si
 * confrontano i BYTE mascherando la sola riga `processed:`, che è l'unica a cambiare a ogni
 * sync anche quando il contenuto è identico. Mascherare l'intestazione intera renderebbe il
 * confronto cieco proprio sul caso per cui esiste (vedi buildLanguageHeader.js).
 *
 * NON cattura gli errori di scrittura: i chiamanti li raccontano in modi diversi
 * ("writing in ...", "while updating ..."), e inghiottirli qui vorrebbe dire che uno dei due
 * smette di dirlo.
 *
 * @param {object} p
 * @param {string} p.filePath
 * @param {string} p.tag
 * @param {boolean} p.isSource
 * @param {object} p.table - la tabella da scrivere
 * @param {string|null} [p.oldText=null] - il testo attualmente su disco. `null` (il default)
 *   significa "non c'è niente di affidabile da confrontare": si scrive sempre.
 * @param {(key: string, value: any) => boolean} [p.isUntranslated] - criterio di "non
 *   tradotta". Omesso vale il default di splitAndSortEntries (valore null), che è quello
 *   giusto per una sub-lingua. **Ometterlo, non passare `null`**: `null` non è `undefined` e
 *   il default non scatterebbe.
 * @param {Date} [p.now]
 * @returns {{ written: boolean, untranslated: [string, any][], text: string }}
 * @throws {Error} se la scrittura fallisce, o se una chiave non rientra nel formato
 */
export default function writeLanguageFileIfChanged({
  filePath, tag, isSource, table, oldText = null, isUntranslated, now = new Date(),
}) {
  const { translated, untranslated } = splitAndSortEntries(table, isUntranslated);
  const text = serializeLanguageFile({ tag, isSource, translated, untranslated, now });

  if (oldText !== null && sameIgnoringProcessed(oldText, text)) {
    return { written: false, untranslated, text };
  }
  fs.writeFileSync(filePath, text, "utf8");
  return { written: true, untranslated, text };
}
```

### 4.3 `lib/dev/vite/updateLanguage.js` — il chiamante "lingua sorgente"

Sostituire il blocco di lettura (righe 59-106) con:

```js
  const letto = readLanguageForSync(filePath);
  let oldText = letto.oldText;

  if (letto.status === "missing") {
    nota = "created";
    baseData = service.sourceTable;
  } else if (letto.status === "unreadable") {
    // Non è un file corrotto da rigenerare: è un file di cui non sappiamo NIENTE. Riscriverlo
    // vorrebbe dire inventarne il contenuto, e il backup sarebbe una copia vuota spacciata per
    // una copia. Le sub-lingue si sincronizzano lo stesso: il loro riferimento è la scansione
    // del codice, non questo file.
    logError(`'${colorize("nome", fileName)}' ${letto.error.message}: left untouched`);
    illeggibile = true;
    nota = "cannot be read, left untouched";
    baseData = service.sourceTable;
  } else if (letto.status === "corrupted") {
    backupLanguageFile(filePath, fileName, letto.error.sourceText ?? null,
      { kind: "corrupted", reason: letto.error.message });
    nota = "was corrupted, rebuilt from the source code";
    baseData = service.sourceTable;
  } else if (letto.status === "empty") {
    // File creato vuoto a mano (bootstrap iniziale): non è corrotto, non c'è nulla da
    // perdere — un backup sarebbe solo rumore.
    nota = "was empty, generated from scratch";
    baseData = service.sourceTable;
  } else if (letto.meta.tableVersion === null) {
    // Un file che si legge ma non porta l'intestazione TableVersion non è una tabella nostra:
    // vale quanto un file corrotto. Il controllo sta QUI e non nel lettore condiviso perché
    // riguarda solo la lingua sorgente — vedi updateAllSubLanguages.js, che non lo fa.
    backupLanguageFile(filePath, fileName, oldText,
      { kind: "corrupted", reason: "no TableVersion header: not a language table" });
    nota = "was corrupted, rebuilt from the source code";
    baseData = service.sourceTable;
    oldText = null; // forza la riscrittura
  } else {
    const newData = service.sourceTable;
    [state, baseData] = updateKeys(letto.table, newData);
    service.renamedKeys = matchRenamedKeys(state, newData);
  }
```

Tre cose da notare, e da riportare in review:

1. `let oldText = letto.oldText` sostituisce le tre assegnazioni a `null` sparse nei rami.
   Solo il ramo `tableVersion === null` deve azzerarlo a mano, perché quel ramo parte da uno
   `status: "ok"`.
2. Nel ramo `tableVersion === null` il backup ora riceve `oldText` invece di `null`. **È un
   miglioramento voluto**: prima quel ramo passava `undefined ?? null`, e il backup ricadeva
   sulla sola `copyFileSync`; con il testo in mano c'è anche il ripiego se la copia non riesce.
   Il comportamento osservabile non cambia se non nel caso in cui `copyFileSync` fallisce.
3. Le stringhe di `nota` sono **identiche** a quelle di prima, carattere per carattere. Sono
   quelle che finiscono nel riepilogo del comando e che `syncPipeline.test.mjs` controlla.

Poi sostituire il blocco di scrittura (righe 129-152) con:

```js
  let written = false;
  // `illeggibile`: il file c'è, non si è potuto leggere, e quindi non si tocca. Senza questa
  // condizione la scrittura partirebbe comunque (oldText è null) e sostituirebbe un contenuto
  // sconosciuto con la sola scansione del codice.
  if (!illeggibile) {
    try {
      ({ written } = writeLanguageFileIfChanged({
        filePath, tag: sourceLanguage, isSource: true, table: baseData, oldText, isUntranslated,
      }));
    } catch (e) {
      logError(`writing in '${colorize("nome", fileName)}': ${e.message}`);
    }
  }
```

> **Differenza minima, dichiarata**: prima il testo veniva serializzato anche quando
> `illeggibile` era vero, e poi buttato. Ora non lo si serializza affatto. L'unico effetto
> possibile è che una chiave fuori formato, su un file illeggibile, non solleva più l'errore di
> `serializeLanguageFile`. È una combinazione che non si dà (le chiavi vengono dalla scansione,
> non dal file), e in ogni caso non scrivere è la decisione giusta in quel ramo.

Aggiornare gli import: togliere `splitAndSortEntries`, `serializeLanguageFile`,
`sameIgnoringProcessed`, `readLanguageFile`, `readLanguageText`; aggiungere
`readLanguageForSync` e `writeLanguageFileIfChanged`. `fs` serve ancora? **No** — controllare:
dopo la sostituzione `fs` non è più usato in questo file, quindi va tolto anche l'import di `fs`.

### 4.4 `lib/dev/vite/updateAllSubLanguages.js` — il chiamante "sub-lingua"

Sostituire il blocco di lettura (righe 83-119) con:

```js
    const letto = readLanguageForSync(sourceFile);
    let oldText = letto.oldText;
    let existingJson;

    if (letto.status === "unreadable") {
      // Non sappiamo cosa contenga: rigenerarlo vorrebbe dire inventarne il contenuto, e il
      // backup sarebbe una copia vuota spacciata per una copia. Si dice e si lascia dov'è.
      logError(`'${colorize("nome", file)}' ${letto.error.message}: left untouched`);
      return null;
    }
    if (letto.status === "corrupted") {
      backupLanguageFile(sourceFile, file, letto.error.sourceText ?? null,
        { kind: "corrupted", reason: letto.error.message });
      nota = "was corrupted, rebuilt";
      existingJson = {};
    } else if (letto.status === "missing" || letto.status === "empty") {
      // File creato vuoto a mano per aggiungere una lingua nuova: non è corrotto, non c'è
      // nulla da perdere — un backup sarebbe solo rumore.
      nota = "new language, was empty";
      existingJson = {};
    } else {
      existingJson = letto.table;
      // Zero voci non è un errore del parser: distinguere "tabella legittimamente vuota" da
      // "file svuotato a mano" richiede di sapere quante chiavi ha il codice, e lo sa solo chi
      // chiama. Se il riferimento ha chiavi e il file no, il file è stato svuotato: si mette
      // al sicuro prima di ripopolarlo. Qui il backup conta davvero, perché qui si perdono
      // traduzioni.
      if (Object.keys(existingJson).length === 0 && Object.keys(referenceTable).length > 0) {
        backupLanguageFile(sourceFile, file, oldText,
          { kind: "corrupted", reason: "no entry found: the file has content but not a single key" });
        nota = "was emptied, rebuilt";
        existingJson = {};
        oldText = null; // forza la riscrittura
      }
    }
```

> **Attenzione a due dettagli.** Il primo: il ramo `"missing"` non esisteva prima — la vecchia
> lettura chiamava `readLanguageText` su un file che il ciclo chiamante ha appena elencato da
> `listLanguageFiles`, quindi c'è sempre. Trattarlo come `"empty"` è la scelta giusta e non
> cambia niente in pratica. Il secondo: nel ramo "svuotato" `oldText` va azzerato a mano, come
> in `updateLanguage`, perché si parte da uno `status: "ok"`. Prima quel ramo non lo azzerava —
> e infatti riscriveva comunque, perché la tabella cambiava. Azzerarlo rende esplicita
> l'intenzione senza cambiare il risultato.

Poi sostituire il blocco di scrittura (righe 147-156) con:

```js
        const { untranslated } = writeLanguageFileIfChanged({
          filePath: sourceFile, tag, isSource: false, table: existingJson, oldText,
        });
```

e più sotto `return { tag, missing: untranslated.length, note: nota };` resta com'è. Il
`try/catch` esterno che logga `while updating '...'` **resta**, ed è ciò che raccoglie un
eventuale errore di scrittura.

Aggiornare gli import come al 4.3. Anche qui, controllare se `fs` serve ancora: dopo la
sostituzione **non serve più**, va tolto.

### 4.5 Gli altri due chiamanti della terna

- `lib/dev/vite/vitetranslate.js`, dentro `bootstrapSubLanguage` (righe 294-302):
  ```js
  const { untranslated } = writeLanguageFileIfChanged({
    filePath, tag, isSource: false, table: subData,
  });
  tableByTag[tag] = subData;
  tags.push(tag);
  return untranslated.length;
  ```
  (`oldText` omesso → scrive sempre, che è il comportamento di adesso.)
  Togliere gli import ora inutili di `splitAndSortEntries` e `serializeLanguageFile`.
  **Nota**: questa modifica va fatta qui e non rimandata al Punto 6, perché al Punto 6 questa
  funzione si sposta in un altro file e va spostata già nella forma finale.

- `lib/dev/vite/uty/migrateLegacyLanguages.js` (righe 76-84):
  ```js
  writeLanguageFileIfChanged({
    filePath: targetPath, tag, isSource: tag === sourceLanguage, table,
  });
  fs.renameSync(legacyPath, `${legacyPath}.bak-migrated-${Date.now()}`);
  ```
  L'ordine scrivi-poi-rinomina non cambia. `fs` qui serve ancora (readdir, rename, writeFileSync
  del ramo "file vuoto"): non toglierlo.
  Togliere gli import di `splitAndSortEntries` e `serializeLanguageFile`.

### 4.6 Verifica del punto

`node test/run.mjs` → 25/25, con attenzione a `syncPipeline` (86 asserzioni) e `headerContract`
(11). Sono i due che coprono esattamente questo codice.

---

## Punto 5 — i due helper piccoli

### 5.1 `lib/dev/vite/uty/posix.js`

Nuovo file:

```js
// Architettura d'insieme: doc/structure.md § "Fase 1 — Precompilazione: il comando di sync".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

/**
 * Il percorso con i separatori di Windows normalizzati a "/".
 *
 * Serve ovunque un percorso del filesystem debba essere confrontato con un id di Vite (che è
 * sempre posix) o scritto in un messaggio. Era la stessa espressione ripetuta a mano in nove
 * punti: non è un rischio di divergenza, è rumore che nasconde cosa la riga sta davvero
 * facendo.
 */
export const toPosix = (p) => p.replace(/\\/g, "/");
```

Usarlo in:

- `lib/dev/vite/vitetranslate.js`, cinque punti: righe 50 (`OWN_LIB_DIR`), 135
  (`localeDirPosix`), 246 (`pathOf`), 462 e 582/587 (i guard sull'id nel transform).
- `lib/dev/vite/uty/shortPath.js`, riga 26.
- `lib/dev/vite/uty/walkSource.js`, riga 48.

### 5.2 Cosa NON toccare, e perché

`lib/dev/babel/markerCore.js`, funzione `relPathOf` (riga 133): la stessa espressione, ma quel
percorso entra nel **checksum dell'id** di ogni marcatore. È matematicamente identico
importare la funzione, ma resta l'unico punto in cui un errore di battitura cambierebbe ogni
chiave di traduzione del progetto, e il beneficio è una riga. Si lascia dov'è, con questo
commento accanto:

```js
  // Non usa toPosix di uty/posix.js di proposito: questo percorso entra nel checksum degli id,
  // e il lato babel non deve dipendere dal lato vite per una sostituzione di due caratteri.
```

### 5.3 `lib/utility.js` — la riga `$ comando`

Aggiungere in fondo al file:

```js
/**
 * Un comando da digitare in un terminale, su una riga tutta sua.
 *
 * "$ " davanti, come un prompt di shell: è la convenzione più universale per dire "questo si
 * digita", la stessa di ogni man page e ogni README. Il prompt è attenuato ("normale" = dim)
 * perché il prompt non è l'informazione: è il comando che deve saltare all'occhio.
 *
 * Riga a sé, e non in coda alla frase che lo introduce: incollato in mezzo al testo, il wrap
 * lo spezza a metà — era esattamente il difetto di prima, con "...to see which" separato da
 * "files". E non un "\n" dentro lo stesso logEchoColored, che lo stamperebbe come
 * continuazione, rientrato di due in più (vedi HANG qui sopra).
 *
 * Esisteva in tre copie — cli.js, vitetranslate.js, languageStatus.js — ognuna con questa
 * stessa spiegazione scritta accanto.
 *
 * @param {string} comando - il comando per intero, "npx" compreso
 */
export function logCommand(comando) {
  logEchoColored("", `${colorize("normale", "$")} ${colorize("ok", comando)}`);
}
```

Sostituire nei tre punti, cancellando i commenti che ora vivono in `utility.js`:

- `lib/dev/vite/cli.js:447` → `logCommand(\`npx ${CLI_NAME} --status\`);`
- `lib/dev/vite/vitetranslate.js:695` → `logCommand(fixCommand);`
- `lib/dev/vite/uty/languageStatus.js:181` → `logCommand(\`npx ${CLI_NAME} --status\`);`

### 5.4 Verifica del punto

`node test/run.mjs` → 25/25. `logFormat` (69 asserzioni) e `fastVerifyCli` (13) sono quelli che
vedono l'output: **l'output deve essere identico byte per byte**, non "equivalente".

---

## Punto 6 — spacchettare `vitetranslate.js`

786 righe che tengono insieme quattro cose. Si estraggono tre moduli; **non si cambia una riga
di logica**: si sposta, si passa quello che serve, si ricontrolla che i test passino.

Regola generale per tutti e tre: il codice spostato si sposta **verbatim**, commenti compresi.
Se ti viene voglia di migliorare qualcosa mentre lo sposti, non farlo: appuntalo e portalo alla
fase 4.

### 6a — `lib/dev/vite/uty/setupFailure.js`

**Cosa ci si sposta**: la funzione `setupErrorText` (righe 51-108, con tutto il suo JSDoc), la
funzione `scaricaStdout` (righe 28-40) e il blocco che stampa il fallimento di setup, oggi
dentro `configureServer` (righe 675-700).

```js
// Architettura d'insieme: doc/structure.md § "Fase 3 — The virtual module and code splitting",
// "Startup check: stopping before the first request".
// Quel documento è la fonte di verità sul funzionamento della libreria: se cambi il
// comportamento di questo file, aggiornalo nello stesso commit.

import { LEGACY_LANG_EXT, languageFileName } from "./languageFileFormat.js";
import { CLI_NAME } from "./cliName.js";
import { readSession } from "./sessionStore.js";
import { logError, logEchoColored, logCommand, colorize } from "../../../utility.js";

export function setupErrorText(...) { /* verbatim da vitetranslate.js */ }

/**
 * Stampa il blocco di un setup che non parte: cosa manca, dove stavano le tabelle l'ultima
 * volta, cosa dice vite.config, e la cura. Sta qui e non in vitetranslate.js perché è output,
 * e l'output della libreria vive accanto al resto dell'output (vedi languageStatus.js).
 */
export function logSetupFailure({ result, localeDirLabel, sourceLanguage, viteConfigFile, baseDir }) {
  /* verbatim: le righe 679-700 di vitetranslate.js */
}

export function scaricaStdout(timeoutMs = 200) { /* verbatim */ }
```

**In `vitetranslate.js`**, `configureServer` diventa:

```js
      const result = checkSetup({ localeDir, localeDirLabel: defs.localeDir, sourceLanguage: defs.sourceLanguage });
      if (!result.ok) {
        setupFallito = true;
        logSetupFailure({
          result, localeDirLabel: defs.localeDir, sourceLanguage: defs.sourceLanguage,
          viteConfigFile, baseDir,
        });
        // [i commenti su perché non basta un throw restano qui, verbatim]
        await server.close();
        await scaricaStdout();
        process.exitCode = 1;
        process.exit(1);
        return;
      }
```

`buildStart` continua a usare `setupErrorText`, ora importata.

### 6b — `lib/dev/vite/buildManifest.js`

**Cosa ci si sposta**: `EMPTY_FILE` (riga 113), la cache `manifestTables` con il suo commento
lungo (righe 178-200), `readTableForManifest` (202-218), `generateLanguagesModule` (220-410,
`bootstrapSubLanguage` compresa) e `partiallyTranslated` (412-440).

Va scritto come **fabbrica**, perché deve leggere stato che vive in `vitetranslate.js` e che
viene deciso più tardi (in `configResolved`):

```js
/**
 * @param {object} p
 * @param {object} p.defs - le opzioni del plugin così come le ha scritte l'utente
 * @param {string} p.localeDir - percorso assoluto già risolto
 * @param {object} p.reporter - il raccoglitore di avvisi (vedi uty/devReporter.js)
 * @param {() => { isProduction: boolean, errorSolve: object }} p.leggiStato - lo stato che si
 *   risolve solo in configResolved. Una funzione e non due valori: `generate` gira dopo
 *   `configResolved`, ma la fabbrica viene chiamata prima, e catturare i valori adesso
 *   vorrebbe dire catturare quelli provvisori.
 * @returns {{ generate: () => Promise<string> }}
 */
export default function creaManifest({ defs, localeDir, reporter, leggiStato }) {
  const manifestTables = new Map();
  function readTableForManifest(filePath) { /* verbatim */ }
  function partiallyTranslated(tableByTag, tags, errorSolve) { /* verbatim, vedi sotto */ }
  async function generate() { /* ex generateLanguagesModule, verbatim */ }
  return { generate };
}
```

Le tre sostituzioni da fare dentro il codice spostato, e sono le uniche:

1. In cima a `generate()`: `const { isProduction, errorSolve } = leggiStato();`
2. `resolvedErrorSolve` → `errorSolve` (compare in due punti: l'export `errorSolve` del modulo
   virtuale, e la chiamata a `partiallyTranslated`).
3. `marksNotFullyTranslated()` dentro `partiallyTranslated` → `errorSolve.notFullyTranslated !== ""`.
   `partiallyTranslated` riceve `errorSolve` come terzo parametro.

**In `vitetranslate.js`**: si costruisce la fabbrica accanto al reporter,

```js
  const manifest = creaManifest({
    defs, localeDir, reporter,
    leggiStato: () => ({ isProduction, errorSolve: resolvedErrorSolve }),
  });
```

e `load()` diventa `return { code: await manifest.generate(), moduleType: "js" };`.

`marksUntranslated` e `marksNotFullyTranslated` **restano** in `vitetranslate.js`: il primo
serve al transform del Punto 6c, il secondo al watcher di `configureServer`. Non sono logica da
condividere, sono un confronto con la stringa vuota.

### 6c — `lib/dev/vite/compileLocale.js`

**Cosa ci si sposta**: l'oggetto `localeCompiler` (righe 442-530) e la funzione `localeFileRe`
(righe 782-786).

```js
export default function creaCompileLocale({ defs, localeDirPosix, reporter, localeModuleIds, leggiStato }) {
  return {
    name: "vitetranslate:compile-locale",
    enforce: "pre",
    transform: {
      filter: { id: localeFileRe(localeDirPosix) },
      handler(code, id) { /* verbatim */ },
    },
  };
}
```

> [!CAUTION]
> **`handler` deve restare una `function` scritta come metodo dell'oggetto letterale.** Dentro
> usa `this.addWatchFile?.(sourcePath)`, dove `this` è il plugin context che Rollup/Rolldown
> inietta alla chiamata. Trasformarla in una arrow function la fa compilare, la fa passare
> quasi tutti i test, e **rompe in silenzio l'invalidazione del modulo compilato quando cambia
> la lingua sorgente**: in dev la pagina ricaricata continuerebbe a servire le traduzioni
> vecchie. Stessa regola per non estrarla in una funzione dichiarata fuori dall'oggetto e
> assegnata (`handler: miaFunzione`): quella andrebbe bene, una arrow no.

Le sostituzioni dentro il codice spostato:

1. In cima a `handler`: `const { errorSolve } = leggiStato();`
2. `resolvedErrorSolve.absentDataInArray` → `errorSolve.absentDataInArray`
3. `marksUntranslated()` → `errorSolve.untranslated !== ""`

**In `vitetranslate.js`**:

```js
  const localeCompiler = creaCompileLocale({
    defs, localeDirPosix, reporter, localeModuleIds,
    leggiStato: () => ({ errorSolve: resolvedErrorSolve }),
  });
```

e in fondo `return [localeCompiler, plugin];` resta identico.

### 6d — Cosa resta in `vitetranslate.js`

Gli import, la validazione delle opzioni, la lettura case-insensitive di `simpleLog`, le
variabili di stato (`setupFallito`, `includeFallback`, `emitSourceMaps`, `isProduction`,
`viteConfigFile`, `errorSolveOptions`, `resolvedErrorSolve`), `marksUntranslated` /
`marksNotFullyTranslated`, `localeModuleIds`, `ownDir` / `OWN_LIB_DIR`, le due chiamate alle
fabbriche, l'oggetto `plugin` con i suoi hook, e il `return`.

**Obiettivo**: sotto le 350 righe. Se resta sopra le 400, qualcosa non si è spostato:
riguardare i tre elenchi "cosa ci si sposta".

### 6e — Verifica del punto

`node test/run.mjs` → 25/25. I test che caricano `vitetranslate.js` come modulo sono
`errorSolve`, `languageResource` e `preloadRule`: se il default export cambia forma se ne
accorgono subito.

---

## Punto 7 — spacchettare `cli.js`

Stessa regola: si sposta verbatim, non si migliora niente strada facendo.

### 7a — `lib/dev/vite/uty/loadConfig.js`

**Cosa ci si sposta**: `consiglioPacchetto` (righe 28-36), `loadExtractMarkers` (37-55),
`loadConfig` (57-113).

Esporta `loadConfig` come default e `loadExtractMarkers` come export nominato (serve al 7b).

### 7b — `lib/dev/vite/uty/scanSource.js`

**Cosa ci si sposta**: `scanSource` (righe 114-200), con tutto il suo JSDoc.

Importa `loadExtractMarkers` da `./loadConfig.js`, `walkSource`, `shortPath`, `hash` da
`../../babel/markerCore.js`, `colorize` da `../../../utility.js`, e `SOURCE_OPEN` da
`../../../markerSyntax.js` (il Punto 1.10 ha già cambiato quella riga).

### 7c — `lib/dev/vite/uty/syncReport.js`

**Cosa ci si sposta**: `printHeader` (201-228), `testoMotivo` (230-260), `nomeLingua`
(262-290), `printSyncSummary` (292-320).

È output, e va accanto a `languageStatus.js`, dove sta già il resto dell'output del comando.
Esporta tutte e quattro (`testoMotivo` e `nomeLingua` servono solo dentro, ma `testoMotivo` lo
chiama `main()`, quindi va esportata).

### 7d — Cosa resta in `cli.js`

Lo shebang, gli import, `collectAddTags`, `BCP47_URL`, `HELP`, `main()` e `main().catch`.

**Obiettivo**: sotto le 330 righe.

### 7e — Verifica del punto

`node test/run.mjs` → 25/25. `fastVerifyCli` e la coda di `syncPipeline` lanciano `cli.js` come
processo e ne confrontano l'output: sono la prova che nulla è cambiato.

---

# FASE 2 — Test

Solo adesso si scrivono i test. Prima si rilegge `4_1_1.necessarytest.md` e ci si aggiunge
tutto ciò che è emerso in fase 1.

## 2.1 Test esistenti

Devono restare **25/25, 1114 asserzioni**, con una sola modifica prevista: l'import di
`compiledMarker` in `test/list/babelTranslateReference.mjs` (già fatta al Punto 1.11).

Se un altro test va toccato, **è un ask all'utente**, non una correzione da fare: vuol dire che
qualcosa è cambiato nel comportamento, e questo piano dice che niente deve cambiare.

## 2.2 Test nuovi

Quattro, tutti nello stile dei file già presenti in `test/list/` (stesso helper `eq`, stessa
intestazione a commento che dice cosa il file mette alla prova). Aggiungerli a
`test/run.mjs` come gli altri.

**`test/list/markerSyntax.test.mjs`** — la prova che chi scrive e chi legge il marcatore
usano la stessa sintassi. È il test che avrebbe intercettato la divergenza per cui il Punto 1
esiste:

- `compiledMarker("App_k", "ciao", true)` → `markerKey` restituisce `"App_k"` e
  `markerFallback` restituisce `"ciao"`.
- `compiledMarker("App_k", "ciao", false)` → `markerKey` restituisce `"App_k"` e
  `markerFallback` restituisce `undefined`.
- `isCompiledMarker` è vero su entrambe le forme prodotte da `compiledMarker` e falso su
  `"_%_ciao_%_"`, su `"ciao"`, su `"_<_ciao"`, su `"ciao_>_"`.
- Un testo avvolto da `SOURCE_OPEN`/`SOURCE_CLOSE` passa `markedTextOf` e `stripSourceMarker`
  lo riduce al contenuto.
- `UNTRANSLATED_KEY` non può collidere con una chiave vera. Attenzione a come si asserisce:
  `__untranslated__` **rientra** nel pattern delle chiavi di `LANGUAGE_KEY_RE`, perché comincia
  per `_`, quindi non è quella la prova. La prova è che nessun id generato può valere così:
  `sanitizeName` toglie ogni carattere fuori da `[A-Za-z0-9]` e l'id finisce sempre con
  `_` più il checksum in base 36. Verificarlo passando per `registerMarker` su una manciata di
  testi: l'id prodotto non è mai `UNTRANSLATED_KEY`.

**`test/list/languageKeyFormat.test.mjs`** — lettore e scrittore accettano la stessa chiave:

- per un elenco di chiavi (valide: `App_abc`, `_x`, `n1_z`, `a.b-c`; non valide: `1abc`,
  `a:b`, `a b`, `""`, `à`), `LANGUAGE_KEY_RE.test(k)` e il fatto che
  `serializeLanguageFile` accetti o rifiuti quella chiave devono coincidere;
- e per ogni chiave valida, il file prodotto da `serializeLanguageFile` si rilegge con
  `parseLanguageFile` senza errori, con lo stesso valore.

**`test/list/readLanguageForSync.test.mjs`** — i cinque stati su file veri in una cartella
temporanea:

| Preparazione | `status` atteso |
|---|---|
| file non creato | `"missing"` |
| una **cartella** con quel nome | `"unreadable"` |
| file con una riga fuori formato | `"corrupted"`, e `error.sourceText` è il testo del file |
| file vuoto (zero byte) | `"empty"` |
| file valido | `"ok"`, con `table` e `meta.tableVersion` |

Più l'asserzione che vale per tutti: **`oldText` è `null` in ogni caso tranne `"ok"`**. È
l'invariante che il modulo esiste per garantire.

**`test/list/writeLanguageFile.test.mjs`**:

- `oldText` omesso → `written: true`, il file esiste e si rilegge;
- `oldText` uguale al testo prodotto tranne la riga `processed:` → `written: false`, e il file
  su disco **non** è stato toccato (confrontare l'mtime, o non creare il file affatto e
  verificare che non venga creato);
- `oldText` che differisce sulla riga `missing key:` → `written: true` (è il caso che
  `sameIgnoringProcessed` deve vedere, ed è per questo che non maschera l'intestazione intera);
- `isUntranslated` omesso → il criterio è "valore null"; `isUntranslated` passato → è quello a
  decidere;
- una chiave fuori formato → la funzione **lancia** (non cattura), così il chiamante può
  raccontarlo a modo suo.

## 2.3 Verifica della fase

`node test/run.mjs` → 29/29 test, e il numero di asserzioni sale. Nessun test preesistente
modificato oltre a `babelTranslateReference.mjs`.

---

# FASE 3 — Build

Prima volta che si costruisce in tutto il piano.

```bash
npm run build
ls -l lib/dist/*.js lib/dist/*.cjs
```

Confronto con la baseline in cima a questo documento:

| File | Baseline | Atteso |
|---|---|---|
| `react.es.js` | 42.507 B | uguale, o meno |
| `react.cjs` | 44.516 B | uguale, o meno |
| `vitetranslate.es.js` | 85.319 B | uguale ±200 B |
| `vitetranslate.cjs` | 86.761 B | uguale ±200 B |

**Le costanti condivise vengono inlineate dal bundler**, quindi il lato react non deve
crescere. Una crescita **oltre 1 kB su `react.*`** vuol dire che `markerSyntax.js` si sta
trascinando dietro qualcosa che non dovrebbe: rileggere l'invariante 5 e 6 (nessun import,
nessuna `Object.freeze`). Se cresce, **non si prosegue**: si va in fase 4.

Poi la prova sul campo, che è quella che conta davvero perché tocca le tabelle compilate:

```bash
npm run playground:dump    # node test/exampleLangCompile.mjs
npm run playground:build
```

Le tabelle compilate emesse devono essere identiche a prima. Se non c'è un riferimento salvato,
salvarlo **prima** di cominciare la fase 1 (`git stash` + dump + `git stash pop`), oppure
confrontare contro `git show HEAD:` sui chunk del playground già committati.

---

# FASE 4 — Revisione

Si arriva qui se la build ha detto qualcosa, o se in fase 1/2 è emersa una contraddizione col
piano.

In quel caso si crea `4_1_1.necessaryreview.md` con le note integrative e le riscritture
emendative, si sottopone ogni dubbio a **ask** all'utente, e il processo **riparte dalla fase 1**
sui soli punti toccati.

Le tre cose che con maggiore probabilità finiscono qui:

1. **La arrow function del Punto 6c.** Se `translateContainer` o un test del playground mostra
   traduzioni stantie dopo una modifica alla lingua sorgente, è quello.
2. **`ENTRY_RE` costruita male** (Punto 2.2, il doppio backslash). Lo dice `languageFileIO`.
3. **Una `nota` cambiata di una virgola** nel Punto 4. Lo dice `syncPipeline`.

Se invece è tutto verde: si passa oltre senza creare nessun file.

---

# FASE 5 — Documentazione

Si esegue `4_1_1.necessarydoc.md` più questo elenco, che è già noto adesso.

Valgono le REGOLE DI DOCUMENTAZIONE di `AGENTS.md`: testi in **inglese**, rivolti a chi legge,
discorsività minima.

## 5.1 `doc/structure.md` — tre sezioni

**§ "File map"** (riga 78). Il mindmap: aggiungere `markerSyntax.js` sotto `shared`, accanto a
`htmlDialect.js` e `errorSolve.js`. La mappa letterale: aggiungere le righe dei nove file nuovi,
nello stile delle altre (nome, punti di riempimento, una riga di descrizione):

```text
├── markerSyntax.js ............. marker delimiters, %s and __untranslated__ — one source for both sides
```

e sotto `dev/vite/`:

```text
│       ├── buildManifest.js .... the virtual module content (languages, preloads, fallback table)
│       ├── compileLocale.js .... the "vitetranslate:compile-locale" transform
```

e dentro `uty/` — la riga di `uty/` oggi è una sola descrizione di gruppo, quindi si può
lasciare così o elencare i nuovi: `cliName.js`, `loadConfig.js`, `posix.js`,
`readLanguageForSync.js`, `scanSource.js`, `setupFailure.js`, `syncReport.js`,
`writeLanguageFile.js`.

La "Quick reading rule" in fondo alla sezione dice che i file condivisi fra i due mondi sono
**due**: diventano **tre**.

**§ "Intermediate files, in order"** (riga 736): controllare se nomina `vitetranslate.js` e
`cli.js` come contenitori di cose che ora stanno altrove, e aggiornare i rimandi.

**§ "Invariants not to break"** (riga 849): aggiungere un invariante nuovo, nello stile degli
altri, sul fatto che i delimitatori del marcatore e le due chiavi riservate vivono in
`lib/markerSyntax.js` e **non si riscrivono a mano** da nessuna parte. Numerarlo come il
successivo disponibile.

## 5.2 Le intestazioni dei file nuovi

Ogni file nuovo porta l'intestazione a tre righe con il rimando alla sezione giusta di
`structure.md`, come tutti gli altri. Sono già scritte nei blocchi di codice di questo piano:
copiarle.

## 5.3 Le intestazioni dei file che hanno perso pezzi

`vitetranslate.js` e `cli.js` citano in testa le sezioni di cui parlano. Verificare che il
rimando sia ancora giusto ora che parte del contenuto è altrove.

## 5.4 Cosa NON toccare

`README.md` non cambia: non è cambiato niente di visibile a chi usa la libreria. Stessa cosa
per `doc/api.md`, `doc/cli.md`, `doc/plugin-options.md`, `doc/diagnostics.md`. Se ti sembra che
uno di questi vada aggiornato, quello è il segnale che qualcosa è cambiato nel comportamento:
**ask all'utente**.

---

# FASE 6 — Pulizia

Rimuovere `4_1_1.necessarytest.md`, `4_1_1.necessarydoc.md` e `4_1_1.necessaryreview.md` se
esistono e non servono più. Resta solo questo file.

Ultima passata sui sorgenti: cercare i commenti rimasti orfani, cioè quelli che dicono "vedi
X" dove X non contiene più quella cosa.

```bash
grep -rn "vedi cli.js\|vedi vitetranslate.js\|vedi lì CLI_NAME" lib --include=*.js | grep -v "^lib/dist"
```

E la verifica che nessuna delle costanti deduplicate sia ricomparsa a mano:

```bash
grep -rn '"_%_"\|"_<_"\|"_>_"\|"__untranslated__"\|"vtranslate-cli"' lib --include=*.js --include=*.jsx | grep -v "^lib/dist" | grep -v "markerSyntax.js\|cliName.js"
```

Deve restituire **solo** righe di commento. Se restituisce codice, quella costante è tornata a
essere scritta due volte e il piano non è finito.

---

# FASE 7 — logDiary

Aggiungere una nota `> [!TIP]` **in testa a questo documento**, subito sotto la nota per il
revisore umano, che riassuma in estrema sintesi cosa è successo nelle sette fasi: elenco
puntato, massimo una decina di righe. Se lungo la strada è emersa una scelta pericolosa o un
avvertimento, usare `> [!IMPORTANT]` o `> [!CAUTION]` invece.

Cose che vanno certamente nel log, se sono successe:

- se il `dist` è cresciuto o calato, di quanto e perché;
- se un test preesistente è stato toccato, quale e con quale ask;
- la scelta del Punto 5.2 (`relPathOf` lasciata fuori dalla deduplica) se qualcuno la
  rimetterà in discussione;
- il numero finale di righe di `vitetranslate.js` e `cli.js`, contro le 786 e 623 di partenza.
