# Copy this file to `.env.local` (already git-ignored) and paste your key after the `=`.
# The key itself never goes into vite.config.* — there goes only the NAME of the
# variable, through `llm.connection.apiKeyEnv`.
RESTAURANT_API_KEY=sk-................................